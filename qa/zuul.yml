zuul:
  routes:
    notification:
      path: /api/notification/**
    user:
      path: /api/user/**
    cms:
      path: /api/cms/**
    catalog:
      path: /api/catalog/**

ribbon:
  eureka:
    enabled: true

server:
  port: 8002
